name: Build and Push Fixed Docker Image

on:
  push:
    branches:
      - fix/telegram-getupdates-http-method
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  OFFICIAL_VERSION: "0.6.9"

jobs:
  build-amd64:
    name: Build amd64 Binary
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y libudev-dev pkg-config

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.94

      - name: Ensure web/dist placeholder exists
        run: mkdir -p web/dist && touch web/dist/.gitkeep

      - name: Exclude tauri from workspace
        run: sed -i 's/"apps\/tauri"[, ]*//' Cargo.toml

      - name: Build release
        run: cargo build --release --features "channel-lark,whatsapp-web"
        env:
          CARGO_TARGET_X86_64_UNKNOWN_LINUX_GNU_LINKER: clang
          CARGO_TARGET_X86_64_UNKNOWN_LINUX_GNU_RUSTFLAGS: "-C link-arg=-fuse-ld=lld"

      - name: Check binary size
        run: |
          size=$(stat -c%s target/release/zeroclaw)
          echo "Binary size: $size bytes"
          if [ "$size" -lt 1000000 ]; then
            echo "ERROR: binary too small (${size} bytes)"
            exit 1
          fi

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: zeroclaw-binary-amd64
          path: target/release/zeroclaw
          retention-days: 1

  download-arm64:
    name: Download arm64 Binary
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Download official arm64 binary
        run: |
          mkdir -p bin/arm64
          curl -fsSL "https://github.com/zeroclaw-labs/zeroclaw/releases/download/v${OFFICIAL_VERSION}/zeroclaw-aarch64-unknown-linux-gnu.tar.gz" \
            -o /tmp/zeroclaw-arm64.tar.gz
          tar xzf /tmp/zeroclaw-arm64.tar.gz -C bin/arm64

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: zeroclaw-binary-arm64
          path: bin/arm64/zeroclaw
          retention-days: 1

  build-image:
    name: Build Docker Image
    needs: [build-amd64, download-arm64]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Download amd64 binary
        uses: actions/download-artifact@v4
        with:
          name: zeroclaw-binary-amd64
          path: bin/amd64/

      - name: Download arm64 binary
        uses: actions/download-artifact@v4
        with:
          name: zeroclaw-binary-arm64
          path: bin/arm64/

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version
        id: version
        run: |
          VERSION=$(sed -n 's/^version = "\([^"]*\)"/\1/p' Cargo.toml | head -1)
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Prepare docker context
        run: |
          mkdir -p docker-ctx/bin/amd64 docker-ctx/bin/arm64
          mkdir -p docker-ctx/zeroclaw-data/.zeroclaw docker-ctx/zeroclaw-data/workspace
          cp bin/amd64/zeroclaw docker-ctx/bin/amd64/
          cp bin/arm64/zeroclaw docker-ctx/bin/arm64/
          cp Dockerfile.ci docker-ctx/Dockerfile
          cp Dockerfile.debian.ci docker-ctx/Dockerfile.debian
          printf '%s\n' \
            'workspace_dir = "/zeroclaw-data/workspace"' \
            'config_path = "/zeroclaw-data/.zeroclaw/config.toml"' \
            'api_key = ""' \
            'default_provider = "openrouter"' \
            'default_model = "anthropic/claude-sonnet-4-20250514"' \
            'default_temperature = 0.7' \
            '' \
            '[gateway]' \
            'port = 42617' \
            'host = "[::]"' \
            'allow_public_bind = true' \
            > docker-ctx/zeroclaw-data/.zeroclaw/config.toml

      - name: Build and push Debian image (multi-platform)
        uses: docker/build-push-action@v6
        with:
          context: docker-ctx
          file: docker-ctx/Dockerfile.debian
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.version.outputs.version }}-telegram-get-fix
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:telegram-get-fix-latest
          platforms: linux/amd64,linux/arm64
